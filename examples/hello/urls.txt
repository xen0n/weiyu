--exact
--renderer=mako

/static/:
    --regex
    ^(?P<path>.*)$ staticfile null

/calc/:
    --regex
    ^(?P<val>\d+)/:
        --regex
        text/$ multiformat-test
        json/$ multiformat-test json

/ajax/:
    --include=urls.ajax.txt

"" index
